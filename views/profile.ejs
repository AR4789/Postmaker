<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class=" bg-zinc-900">
    <div class="w-full  text-white p-10">
        <div class="flex justify-end ww-full">
        <a href="/logout" class="bg-red-500 hover:bg-red-700 font-bold py-2 px-4 rounded-md inline-block mb-5">Logout</a>
        </div>
        <h1 class="text-3xl mb-5"><span class="font-light">Hello, <%= user.name %> 👋</span></h1>
        <h5 class="mb-5">you can create a new post</h5>     
        <form action="/post" method="post">
            <textarea class="p-3 mb-3 outline-none w-1/3 border-2 border-zinc-800 bg-transparent resize-none block" placeholder="What's on your mind" name="content"></textarea>
            <input type="submit" value="Create Post" class="rounded-md border-2 border-zinc-800 bg-blue-500 block px-3 py-2 w-40">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer">
                <% user.posts.reverse().forEach(function(post) {%>
                    <div class="post w-1/3 p-5 border-[1px] mb-4 border-zinc-700 rounded-md bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="mb-5 text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-4 inline-block"><%= post.likes.length%></small>

                        <div class="btns flex gap-4 mt-1">
                            <a href="/like/<%= post._id %>" class="text-blue-500 hover:text-blue-700">
                                <%= post.likes.indexOf(user._id) === -1  ?"Like" : "Unlike" %>


                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-600 hover:text-zinc-400">Edit</a>
                        </div>
                    </div>

                <% }) %>
             

            </div>
        </div>
    </div>
    
</body>
</html>